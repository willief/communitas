import { describe, it, expect } from 'vitest'

// Mock network tests are currently implemented in integration tests
describe.skip('MockNetwork', () => {
  it('should be tested in integration tests', () => {
    expect(true).toBe(true)
  })
})